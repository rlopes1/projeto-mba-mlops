name: ci-cd

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Pipeline CI/CD do pcdf-bo-triagem (esqueleto Aula 01)"
